# Project TODO

Tracks the **11 build-week schedule** (see `PROJECT_UPDATE_BRIEF` §6, mirrored below) against actual repo state. Update this file as tasks land — don't let it drift.

*Last rewritten: 2026-08-07, against the August 2026 scope decisions.*

## Key dates

| Date | Event |
|------|-------|
| **6 Sep 2026** | Modern-city dataset freeze — no additions after this date |
| **23 Oct 2026** | **Results freeze** — no new experiments after this date, writing only |
| **mid-Nov 2026** | Submission deadline (IEEE CoG) |

**Scope note:** healthcare and education domains, and the human-evaluation study, are **cut** (see README). Phases for them have been deleted from this file. The project reports **automatic metrics only**; the absence of a human study is a stated limitation.

**Scope note, 2026-08-08 — medieval cut too, project is modern-city only now.** The dual-era (modern + medieval control) design from 2026-08-07 lasted one day. The medieval dataset/adapters/eval results below are kept on disk, **not deleted**, but are no longer part of the active plan — no medieval-vs-modern comparison, no "control condition" framing anywhere going forward. A Week 2 guard-adapter training run using the medieval dataset was **killed mid-run** (step 3/36) when this decision landed — see Week 2 below for the real scheduling consequence this creates.

---

## Work already banked (carries forward)

Complete and reusable — most is domain-agnostic and survives the modern-city pivot. Full blow-by-blow history is in git and prior revisions of this file.

**Framework / systems (domain-agnostic — carries over untouched):**
- [x] `training/train_adapter.py` — QLoRA pipeline (transformers + peft + trl SFTTrainer). Real fixes banked: bf16-end-to-end training (fp16+bf16 GradScaler crash), step-checkpointing after a thermal-TDR crash, and a **completion-only-loss bug** (loss was computed over the whole templated sequence, not just the assistant response — fixed by emitting `prompt`/`completion` columns).
- [x] `training/blend_adapters.py` — adapter interpolation via PEFT `add_weighted_adapter(combination_type="linear")` (correctly accounts for each adapter's alpha/rank scaling).
- [x] `backend/adapter_manager.py` — `AdapterManager` loads adapters onto one resident base and hot-swaps via `set_adapter()`. **Verified live:** second call with a different archetype reported `adapter_switch_ms: 0` — real proof of the no-reload claim.
- [x] `backend/main.py` — FastAPI `POST /chat`, `GET /domains`, `GET /archetypes`, `GET /health`. Model preloaded at startup.
- [x] `frontend/` — Next.js demo (domain/archetype dropdowns, live chat, live PDM bar, per-message latency). Verified end-to-end in-browser.

**Medieval-fantasy dataset + results (archived 2026-08-08 — out of scope, kept for reference, not deleted):**
- [x] 1,003-pair medieval set, validated (1003/1003 schema-conformant, 0 duplicate ids). Sources: Gutenberg (Shakespeare/Chaucer/Malory, 626), chimbiwide rule-based rewrite (150), hand-authored (227).
- [x] 50-entry stress-test corpus, held out.
- [x] **Key medieval finding (methodology evidence, not a current-scope result):** training-data *composition* matters more than LoRA rank/alpha/epochs for lexically-specific persona control. Eight configs on the full mix produced near-zero archaic markers; training only on the 626 dialect-dense Gutenberg entries (`medieval_r8_gutonly`) fixed it. Held-out (uncontaminated) A-vs-B: marker rate 4.7% → 14.0%, mean drift 0.9766 → 0.9396. Worth keeping in mind for the modern-city training runs (Weeks 7–8) — the same composition effect may recur.
- [x] **Blending sweep:** α=0.2 beat pure gutonly on both marker rate and over-insertion on the medieval set — a genuine sweet spot there. Not assumed to transfer to the modern α-sweep (Weeks 9–10); that sweep is run fresh on modern adapters.
- [x] **Full fine-tune reference:** best raw persona (46.8% marker rate) but worst over-insertion (0.8932) and 11.86 GB on disk vs. 2.3 MB LoRA — a clean systems tradeoff, general enough to still cite for Contribution 4 even though the domain it was measured on is now out of scope. **2.3MB is specific to this archived config (r=8, `q_proj`/`v_proj` only) — do not cite it as "the" adapter size in the paper.** All 8 modern archetype adapters (Weeks 7–8) use a wider config (r=16, 7 target modules) and are **25.3MB** each — checked directly (`adapter_model.safetensors`), not assumed. Still ~470x smaller than the 11.86GB full fine-tune, so the systems tradeoff story holds, just with the real current number instead of the stale one.

> These medieval numbers are **archived, not a current result** — do not report them as project findings without noting they predate the modern-only scope decision. The old PDM baseline mean drift of **0.9833 is saturated and reports nothing useful** regardless of domain — do **not** carry it forward (see PDM v2 note in Week 5).

---

## The 11 build weeks

| Weeks | Focus | Exit condition |
|-------|-------|----------------|
| 1 (Aug 10–16) | llama.cpp/GGUF inference path. Re-measure TinyLlama latency. Qwen3-0.6B setup. | Latency < 500 ms |
| 2 (Aug 17–23) | Train one adapter (guard, 189 pairs). End-to-end generation. | Adapter generates in-character text |
| 3–4 (Aug 24–Sep 6) | Modern dataset, 600 pairs. Annotate visibility sets in the same pass. | **Frozen Sep 6** |
| 5 (Sep 7–13) | PDM v2 against real generations. | Baseline drift shows variance |
| 6 (Sep 14–20) | KBD implementation + planted forbidden facts. | KBD catches ≥1 real violation |
| 7–8 (Sep 21–Oct 4) | Train 8 modern adapters × 2 base models. | 16 adapters exist |
| 9–10 (Oct 5–18) | **α-sweep.** KBD + PDM across α for 3 adapter pairs. | This is the paper — protect these weeks |
| 11 (Oct 19–23) | Flat-RAG baseline at matched token budget. Latency/VRAM benchmarks. Figures. | Freeze |

**Slip absorbs into weeks 3–8. Never out of 9–10.**

---

### Week 1 (Aug 10–16) — Inference path + Qwen3 setup · exit: latency < 500 ms

- [x] **Re-measured TinyLlama latency on the GGUF path — real numbers, done 2026-08-07.** Built `evaluation/run_gguf_latency.py`: downloads `TheBloke/TinyLlama-1.1B-Chat-v1.0-GGUF` (Q4_K_M, 668.8MB) via `huggingface_hub`, serves via `llama_cpp.Llama` (zephyr chat format — verified TinyLlama-Chat's actual template, not ChatML), measures wall-clock per response.
  - **CPU-only** (prebuilt wheel, no compiler toolchain on this machine): **1352ms mean** at `max_tokens=40`, down from Ollama's 3486ms — **2.6x faster**, but still over the 500ms target.
  - **Tried GPU offload** (installed the CUDA `llama-cpp-python` wheel, `n_gpu_layers=-1`) — needed the CUDA runtime DLLs (cudart/cublas), which torch's `cu121` wheel already had bundled (`torch/lib/`), avoided a full CUDA-toolkit install by pointing `PATH` at that directory instead. Verified via verbose load log: **23/23 layers genuinely offloaded to CUDA0 (MX450)**, not a silent CPU fallback. **Result: no speedup** (~1350ms, identical to CPU) — `nvidia-smi` shows this card power-capped at 5W even under load, and for a model this small the GPU's kernel-launch overhead + low throughput at that power ceiling doesn't beat 8-core CPU AVX2 inference. A real, reportable hardware-constraint finding, not a bug left unfixed.
  - **At `max_tokens=20`** (a more game-realistic NPC bark length, not a 40-token paragraph): **816ms mean** — **4.3x faster than the old baseline**, but still misses 500ms. Timing is roughly linear (~27ms/token + ~270ms fixed overhead) — hitting 500ms via token-count alone would mean an ~8-token response, too short to be a usable NPC line.
  - **Honest conclusion:** GGUF quantized CPU inference is a large, real win over the old Ollama path, but TinyLlama-1.1B does not hit <500ms on this hardware at a usable response length regardless of CPU/GPU. Qwen3-0.6B (roughly half the params) was proposed as the next test for this — **explicitly parked by user decision, 2026-08-07: "leave qwen, use tinyllama itself."** Staying single-base (TinyLlama only) for now; revisit Qwen3 later if needed.
  - Noted separately, not fixed (quality issue, not latency): some GGUF outputs leak `"Never break character."` / narrator-voice artifacts into the response — a prompt-template issue, tracked for later, out of scope for the latency task.
  - **Thread/batch tuning** (`n_threads=os.cpu_count()`, `n_batch=512`, explicit `n_gpu_layers=0` instead of relying on defaults) shaved default CPU latency from 1352ms to **1186ms mean** at `max_tokens=40` (~12% faster) — small but real, baked into `run_gguf_latency.py`'s defaults now. Script also self-contained: adds torch's `cu121` DLL dir to `PATH` internally so the CUDA-linked `llama-cpp-python` wheel loads without a manual env-var step every run.
- [x] **Merge a trained LoRA adapter into base weights, export to GGUF Q4_K_M, serve via llama.cpp — done end-to-end, 2026-08-07**, using the existing `medieval_r8_gutonly` control adapter as the vehicle (no modern adapter exists yet).
  - `training/merge_lora.py` (new): loads base in bf16 on CPU (not 4-bit — merging into an already-quantized base would compound error on top of the LoRA delta), `PeftModel.from_pretrained` + `merge_and_unload()`, saves a plain HF checkpoint indistinguishable from a full fine-tune.
  - GGUF conversion: shallow-cloned `ggml-org/llama.cpp` for `convert_hf_to_gguf.py` (pure Python, no compiler needed) into `training/llama.cpp/`. Installed only `gguf`+`sentencepiece` rather than its full pinned `requirements-convert_hf_to_gguf.txt` (which demands `transformers==4.57.6`/`torch==2.11.0`/`numpy~=1.26` — would have downgraded the pinned training stack). Ran clean against the newer versions already installed.
  - Quantization to Q4_K_M: **no compiled `llama-quantize.exe` on this machine** (no C/C++ toolchain — see Known issues). Instead called `llama_cpp.llama_model_quantize()` directly, the same C function the binary would call, exposed through `llama-cpp-python`'s ctypes bindings — `training/quantize_gguf.py` (new). 2.2GB f16 -> 667.8MB Q4_K_M.
  - **Benchmarked the real merged+quantized adapter** (`evaluation/run_gguf_latency.py --model medieval_gutonly`, added a `local_path` option alongside the HF-download path): **777ms mean** at `max_tokens=20`, CPU. Outputs show genuine archaic markers (`"Nay, I will not have a room for the night..."`) with no `"Never break character."` leakage — confirms the merge preserved the trained persona correctly, not just a speed number.
  - Still misses <500ms, consistent with the stock-model finding above — the ceiling is model size + this hardware, not the merge/quantize/serve pipeline itself, which now works correctly end-to-end.
- [ ] Stand up Qwen3-0.6B — **parked**, see above.
- [ ] Ensure dataset formatting + any tokenizer-dependent metric code handles **both** Llama-chat and ChatML templates.

### Week 2 (Aug 17–23) — One adapter, end-to-end · exit: in-character generation

- [x] **LoRA config updated for the new runs** — `training/configs/lora_config.yaml` now `r=16`, `alpha=32`, target `q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj` (was `r=8`, `q_proj, v_proj` only). Applies to all runs going forward; the archived medieval adapters above were trained under the old config and aren't affected retroactively.
- [x] **Added `--archetype` filtering to `train_adapter.py`** (`build_dataset()` now takes an `archetype` param, filters `persona.archetype == X`) — needed either way, for training single-archetype adapters instead of whole-domain ones.
- [x] **Started training a guard adapter (189 medieval entries) as the Week 2 pipeline-validation run — killed mid-run (step 3/36) when the modern-only decision landed 2026-08-08.** This wasn't wasted: it proved the new r=16/7-module config + `--archetype` filter work mechanically (model loaded, dataset built to exactly 189 entries, training started, wandb run `medieval-r16_a32_guard-adapter` logged) before being stopped on purpose, not because of a bug.
- [x] **Blocker resolved 2026-08-08 (option a): hand-authored a 59-entry police-officer pilot batch.** `data/processed/modern_npc_dataset.json`, ids `POL-0001`..`POL-0059`. Original content (no external corpus, no re-voicing), matching how the 227 medieval `SYN-*` entries were made — same reasoning: zero cost, zero IP risk. Tagged `source: hand_authored_pilot`, `week2_pilot` — explicitly documented as a stopgap, **not** a substitute for the real Taskmaster/MultiWOZ/SODA/Synthetic-Persona-Chat 600-pair set still due in Weeks 3–4. `linguistic_markers.dialect_features` left empty (PDM v2's feature families don't exist yet — Week 5 — so nothing honest to put there yet).
- [x] **Trained `modern_r16_a32_policeofficer`** — `train_adapter.py --domain modern --archetype "police officer"`, 59 entries, 3 epochs, 12 steps, r=16/alpha=32/7-module config. `train_loss: 2.289`, `mean_token_accuracy: 0.54`. wandb run `modern-r16_a32_policeofficer-adapter`.
- [x] **Confirmed in-character generation through the GGUF serving path — Week 2 exit condition met.** Full pipeline: `merge_lora.py` → `convert_hf_to_gguf.py` (f16) → `quantize_gguf.py` (Q4_K_M, 667.8MB) → registered as `modern_police` in `run_gguf_latency.py`'s `GGUF_MODELS`. Real outputs, natural contemporary voice, zero leakage: *"Sure, it's just a few blocks down the street."*, *"Just to make sure you're who you say you are."* — no `"Never break character."` artifact seen on the medieval runs.
  - **Incidental result: 535ms mean / 436ms p50** at `max_tokens=20` — the closest any config has come to Week 1's <500ms target (p50 actually clears it). Plausibly real: modern contemporary responses are shorter/terminate faster than archaic phrasing ("Nay, I will not...") did. Not claimed as a clean pass (mean still fails), but worth noting for Week 1's record.
  - `run_gguf_latency.py` generalized: `GGUF_MODELS` entries now carry a `domain` key, prompts/system-template moved into a `PROMPT_SETS` dict keyed by domain instead of one hardcoded medieval-only global — needed to test the modern adapter without regressing the medieval one.
  - `quantize_gguf.py` hit the same CUDA-DLL-on-PATH issue `run_gguf_latency.py` did — fixed the same way (self-contained `_add_torch_cuda_dlls_to_path()`, reuses torch's bundled `cu121` DLLs, no manual env-var step).

### Weeks 3–4 (Aug 24 – Sep 6) — Modern dataset, 600 pairs · **exit: frozen Sep 6**

- [x] **Downloaded all 4 cleared sources, 2026-08-08** — none existed on this machine (they were pulled on a different machine last session, `data/raw/modern/` is gitignored so nothing came through `git pull`). Verified each repo/URL actually existed before pulling anything (`git ls-remote`, `HfApi().dataset_info()`) rather than trusting the brief blind:
  - Taskmaster: `git clone --depth 1 https://github.com/google-research-datasets/Taskmaster.git` → `data/raw/modern/taskmaster/` (1.1GB, TM-1 through TM-4).
  - MultiWOZ: `git clone --depth 1 https://github.com/budzianowski/multiwoz.git` → `data/raw/modern/multiwoz/` (378MB, `data/MultiWOZ_2.2/` is the relevant subfolder).
  - SODA: `huggingface_hub.snapshot_download("allenai/soda")` → `data/raw/modern/soda/` (817MB, parquet).
  - Synthetic-Persona-Chat: `huggingface_hub.snapshot_download("google/Synthetic-Persona-Chat")` → `data/raw/modern/synthetic-persona-chat/` (37MB, parquet).
- [x] **Real correction to `DATA_PIPELINE.md`: MultiWOZ 2.2 has zero "police" domain dialogues.** Inspected all 21 `dialogues_*.json` files directly — actual service distribution is `restaurant 4728, hotel 4182, train 3931, attraction 3485, taxi 1872, hospital 108, bus 6`. No `police`. (It exists in MultiWOZ 1.0/2.0/2.1, apparently dropped from 2.2 — the docs' domain list was aspirational, not verified against the actual downloaded data.) Not used for police officer as originally planned.
- [x] **Built `data/scripts/soda_extractor.py` and extracted the full 600-pair (75×8) target from SODA alone.** SODA's `speakers` field carries the actual in-narrative character names (e.g. "Priest", "Police Officer", "CEO") — searched all 1,191,582 rows for a speaker name matching each archetype's role vocabulary, took (preceding turn, that speaker's turn) as the pair. Verified real counts before building anything: `executive 50252, professor 18425, shopkeeper 16510, service worker 11681, social worker 9920, police officer 8606, bartender 1758, pharmacist 99` matching rows — comfortably enough for 75 each, even pharmacist.
  - **Two real quality bugs caught on manual spot-check, not assumed clean:** (1) bare `officer` in the police-officer pattern matched false positives like "Ski Patrol Officer" and "Loan Officer" — narrowed to `police officer|cop|policeman|policewoman`. (2) SODA embeds inline stage directions like `"(He looks at the prescription.)"` in dialogue text — added a strip pass (`_clean()`) before the word-count quality filter. Re-verified samples after both fixes before merging.
  - Not a heavy rewrite like chimbiwide's `register_rewrite()` — SODA's raw text is already natural contemporary register, so this is extraction + relabeling, documented honestly in each entry's `conversion_note` rather than overclaiming "persona re-voiced."
  - **Result: 600/600 entries merged, ids `SOD-0001`..`SOD-0600`.** Combined with the existing 59-entry hand-authored police pilot from Week 2: **659 total entries**, archetype distribution `police officer 134, [other 7] 75 each`. Police is 59 over the 75 target because Week 2's pilot predates this extraction — not trimmed, more real training data isn't a problem, just means the set is 659 not exactly 600. Validated: unique ids, schema-conformant, 1 negligible duplicate pair (a generic "Hey, how's it going?" greeting appearing in two different source narratives).
- [ ] Taskmaster/MultiWOZ not yet extracted — SODA alone already hit the numeric target, so this is optional diversity/quality enhancement now, not a blocker. Worth doing if archetype coverage or register variety looks thin once PDM v2/KBD can actually evaluate the set (Weeks 5–6).
- [ ] **Annotate visibility sets in the same pass** — flat visibility tags per knowledge item (set intersection, not a tree/graph). Also populate `persona_features[]` per entry. Not started — `linguistic_markers.dialect_features` currently empty on every entry since PDM v2's feature families don't exist yet (Week 5).
- [ ] Extend `stress_test_corpus.json` with **15–20 planted forbidden facts** (probes whose correct answer is a refusal / expression of ignorance) — required for KBD to have signal.
- [ ] **Freeze the dataset on 6 Sep 2026.**

### Week 5 (Sep 7–13) — PDM v2 · exit: baseline drift shows variance

Started early, 2026-08-08 — the blocking prerequisite ("an adapter must exist to calibrate against") was already satisfied by Week 2's `modern_r16_a32_policeofficer` adapter, so no reason to wait for the calendar week.

- [x] **Built `evaluation/pdm_v2.py`** — five feature families instead of one archaic-word-list: (1) **lexicon** — per-archetype distinctive vocabulary computed TF-IDF-style from the real 659-entry dataset (`build_archetype_lexicons()`), not hand-picked; (2) **register** — fixed informal (`gonna/wanna/ain't/...`) vs formal (`therefore/hereby/...`) markers, archetype-independent; (3) **formality** — contraction-density bucket; (4) **syntax** — sentence-length + question-presence buckets; (5) **stance** — hedging vs assertive language. Combined into one feature set per response, same Jaccard-based drift formula PDM v1 used (`single_turn_drift_v2()`) — the aggregation math wasn't the broken part, the single word-list was.
- [x] **Calibrated against real generations, not the dataset** — `evaluation/run_pdm_v2_calibration.py`: 15 real police-officer prompts (sampled across both the hand-authored and SODA-sourced entries, not just the first N), generated via the actual GGUF path, baseline (stock TinyLlama) vs `modern_police` adapter.
  - **Acceptance technically met:** baseline stdev=0.0376 (mean 0.8880, range 0.80–0.92), adapter stdev=0.0103 (mean 0.9173, range 0.88–0.92) — neither is the flat, saturated 0.9833-everywhere failure mode. Real variance exists in both conditions.
  - **Real problem found, not hidden: the ordering is backwards.** The adapter (mean drift 0.9173) scores *worse* than the untrained baseline (mean drift 0.8880), despite the adapter's outputs being obviously better in-character on manual read (*"Just a routine traffic stop, nothing to worry about."*) versus the baseline's meta-commentary artifacts (*"Sure, here's a possible response: NPC: (voice breaking with emotion)..."*). Root cause: the reference feature set is a 25-feature union across the whole archetype's training data, and Jaccard's denominator is dominated by that fixed-size reference — a **short, correctly terse, in-character response has fewer feature slots to hit than a long rambling one**, so brevity gets penalized as drift regardless of actual persona quality. This directly contradicts the earlier finding that in-game NPC lines should be short (`Docs/TODO.md` Week 1 optimization plan).
  - **Not patched blind under time pressure** — this needs a real design decision (length-normalize the Jaccard denominator? weight families instead of pooling into one set? cap per-family match count?) before PDM v2 is trustworthy for the Weeks 9–10 headline α-sweep. Flagged for the user, not silently fixed.
- [x] **Round 2 fix: score each family separately, then average (user decision).** Removed the single pooled feature set — each of the 5 families now gets its own Jaccard, averaged across families. Result: **fixed the length-bias, did not fix the ordering.** Adapter still scored worse (mean 0.8824) than baseline (mean 0.8702), and now the acceptance criterion *failed* too (adapter stdev=0.0034, essentially flat).
  - **Second real problem found via per-family diagnostic breakdown, not guessed:** `formality`/`syntax`/`stance` reference sets were a *union* of every bucket value seen across 134 training entries — for a 3-valued attribute like formality, the union hits all 3 possible buckets almost immediately, so Jaccard(response's one bucket, {all 3 buckets}) is a **constant** regardless of what the response says. Two of five families (register, stance) were contributing zero signal for this exact reason; lexicon was near-zero too, but for the separate, legitimate reason that short responses rarely hit a sparse 15-word archetype vocabulary.
- [x] **Round 3 fix: mode-matching for the three categorical families** (formality/syntax/stance compare against the archetype's single most-common value per attribute, not the union). Real variance came back (baseline stdev=0.0988, adapter stdev=0.0828) — **but the ordering was still backwards.** Per-family breakdown pinpointed exactly why: `formality` mean drift was 0.667 (baseline) vs 0.933 (adapter) — the adapter's contraction-heavy, terse speech ("I'll", "it's", "I'm") didn't match the archetype's formality *mode* ("high" — few contractions).
  - **Third real problem, and this one was about the data, not the code:** checked the actual formality distribution behind that mode — `{high: 62, low: 53, medium: 19}` out of 134. "High" barely edges out "low" as the single most common value; the archetype is genuinely close to bimodal. Binary mode-matching was treating 53/134 (40%) of the archetype's own legitimate training variation as flatly "wrong."
- [x] **Round 4 fix: distributional typicality instead of binary mode-match.** Reference for formality/syntax/stance is now `{value: observed_frequency / peak_frequency}` per attribute — landing on the mode still scores 1.0, landing on a substantial secondary value (like "low" formality, 53/62 ≈ 0.85 of peak) scores proportionally well, landing on something genuinely rare scores low. **Result: correct ordering, finally.** Baseline mean drift **0.4591** (stdev 0.0519) vs adapter mean drift **0.4389** (stdev 0.0147) — the trained adapter now shows *less* drift than the untrained baseline, matching the qualitative read of the actual transcripts. Both conditions still show real, non-degenerate variance.
- [x] **Week 5 closed.** Four calibration rounds, each fix root-caused from a concrete diagnostic (per-family breakdown, actual distribution counts) rather than guessed — matches this project's established practice (train/test contamination, empty-reference-drift artifact from the medieval phase were caught the same way). `pdm_v2.py`'s module docstring documents all three failed attempts and why, so the reasoning isn't lost if this needs revisiting before the Weeks 9–10 α-sweep.
- [ ] Not yet done: run PDM v2 against more than one archetype/adapter pair to confirm this isn't specific to police officer's particular data quirks — worth checking once more adapters exist (Weeks 7–8).

**Follow-up, 2026-08-10 — tried to reduce the adapter's drift score further.** Per-family diagnostic on the Week 5 adapter showed `lexicon` (0.996) and `register` (1.000) were the entire remaining gap — `formality`/`syntax`/`stance` were already near-perfect (0.135/0.063/0.0). User chose to address it with more training data rather than tuning generation params or leaving it (real exposure increase, not gaming the metric).
- [x] **Topped up police officer training data 134 → 300** via `soda_extractor.py --archetype "police officer" --per-archetype 300` — added a `--archetype` flag with existing-output dedup (so a top-up run finds genuinely new SODA matches instead of re-discovering the same first-N rows the original run already merged). Dataset now **825 entries** total (was 659).
- [x] **Retrained `modern_r16_a32_policeofficer` on the expanded set.** Real training signal improvement: loss 2.289 → 1.379, mean_token_accuracy 0.54 → 0.72.
- [x] **Recalibrated end-to-end (merge → GGUF convert → quantize → PDM v2).** Result: baseline mean drift 0.4721, adapter mean drift 0.4431 — gap widened from 0.0202 to **0.0289**, a real if modest improvement, not dramatic. Per-family: `register` improved 1.000 → 0.933 (adapter now occasionally uses a tracked marker, e.g. "yeah"), `lexicon` barely moved 0.996 → 0.978. Best single response hit drift=0.2645 (*"Police officer: Yeah, I'm just looking for my keys..."*) — noticeably below the old floor of ~0.40.
- **Honest read:** 4x the training data bought a small, real gain, not a breakthrough. `lexicon`/`register` staying high even with more data supports the calibration-round finding that short, natural NPC dialogue structurally doesn't pack archetype-specific nouns very often — this may be closer to a ceiling than a data-starvation problem. Worth knowing before assuming "more data" is the answer for the other 7 archetypes in Weeks 7–8 too.

### Week 6 (Sep 14–20) — KBD · exit: catches ≥1 real violation

Started early, 2026-08-10, same reasoning as Week 5 — no need to wait for the calendar week once the prerequisite (a trained adapter) exists.

- [x] **Built `data/processed/knowledge_base.json`** — 17 hand-authored, original world-state facts (2–3 per archetype across all 8), each with a flat `visible_to` archetype list and `signature_phrases` for detection. Set-intersection resolution exactly as specified: `archetype in item["visible_to"]`, no tree/graph/hierarchy.
- [x] **Built `data/processed/kbd_probes.json`** — the "planted forbidden facts" extension flagged as missing since Weeks 3–4. 17 probes targeting the police officer archetype (the only trained adapter): 14 "forbidden" (leading questions about facts visible to a *different* archetype — correct behavior is refusal/ignorance) + 3 "control" (leading questions about police's own visible facts — correct behavior is a real answer, tests KBD doesn't false-positive on legitimate responses).
- [x] **Built `evaluation/kbd_scorer.py`** — `KBD = violations / total_references`, judge-free signature-phrase matching against the knowledge base. Added a negation check before the first calibration run (a response *denying* a fact still contains its signature phrase — "No, I haven't heard about the insulin shipment" — counting that as a violation would be a real, obvious false positive; checked for a negation cue in the same sentence before the matched phrase).
- [x] **First calibration run: exit condition technically passed but recall was clearly too low.** 1/14 forbidden probes flagged (`KBD-08`, a real confirmed violation — the adapter stated a pharmacist-only fact about a flagged prescription). But manual read of the 13 "no reference detected" responses found several that plainly confirm the forbidden fact without literally restating any of its content words — *"Yes, it did. They're still waiting for the shipment to arrive."* confirms the insulin-shipment fact without ever saying "insulin" or "shipment delayed." No phrase list can catch that: there's nothing fact-specific in the response text itself, "it" only resolves to the fact via the probe question, which the general-purpose phrase scorer never sees.
- [x] **Fixed with a probe-aware layer, not a bigger phrase list.** Since every probe already links to a known target fact, added `classify_probe_response()` to `run_kbd_calibration.py` — checks for affirm cues (`yes/yeah/that's right/confirmed`) vs deny cues (`no/not sure/don't have access/haven't heard`) at the response level, specific to calibration (kept `kbd_scorer.py` itself unchanged — it's still correct for its documented purpose, scoring free text with no known probe/target). Conservative by design: ambiguous responses (neither clearly affirm nor deny) don't count as violations, same bias the negation check already used.
- [x] **Real corrected result: 5/14 forbidden probes violated (36%), not 1/14 (7%).** `KBD-01` (insulin shipment), `KBD-05` (bulk discount), `KBD-08` (drug interaction, phrase-matched), `KBD-09` (plagiarism complaint), `KBD-16` (shoplifting ring) — all real confirmations of facts the police officer archetype has no business knowing. The 3 control probes correctly stayed unflagged (police's own knowledge, legitimately answerable). **This is a genuinely serious finding for RQ1/RQ2, not just a metric-passing exercise**: the tiny adapter appears to have almost no epistemic boundary under leading questions — it agrees with the premise of most "is it true that..." questions regardless of whether the fact is one it should plausibly know, a real sycophancy/acquiescence pattern worth naming explicitly for the paper, not just a KBD implementation detail.
- [x] **Exit condition met, honestly this time.**
- [ ] Not yet done: only tested against the police officer archetype (only trained adapter) and only against leading/forbidden-fact probes, not general dialogue (per the Week 6 task's own note that normal dialogue produces near-zero violations, no signal — untested but expected). Revisit once more adapters exist (Weeks 7–8) to see if the 36% acquiescence rate is archetype-specific or a general small-model property.

**Follow-up, 2026-08-10 — tried to reduce the 36% leak rate (user decision: improve it, not just report it).**
- [x] **Root cause was structural, not a training bug: zero refusal examples existed anywhere in the training data.** Nothing ever taught "say you don't know" for an out-of-domain question, so the model had no such behavior to fall back on.
- [x] **Added 25 hand-authored refusal-training examples** (original content, zero API cost, same pattern as every other hand-authored batch this project has used) — **deliberately different specific scenarios than `kbd_probes.json`** (dentist, bakery, gym, vet clinic, etc., not pharmacy/university/bar/social-services/shop/corporate — the actual domains the 17 held-out probes test) so this teaches general epistemic humility, not memorized answers to the evaluation set itself. Same train/test separation discipline as the medieval gutonly contamination catch. Dataset: 825 → 850 entries.
- [x] **Retrained, re-ran the same untouched 17 probes.** Real result: **forbidden-probe violations dropped 5/14 (36%) → 3/14 (21%)** — `KBD-01`, `KBD-05`, `KBD-09`, `KBD-16` now correctly refuse; `KBD-06`, `KBD-08`, `KBD-17` still leak.
- [x] **Real new tradeoff, not hidden: over-refusal on legitimate own-knowledge.** Control probe `KBD-13` (asking about the confidential-informant tip — genuinely police's own knowledge) went from a hedgy-but-acceptable answer to an outright incorrect refusal (*"That's not something I'd know about, sorry."*) after retraining. The refusal behavior generalized slightly too far. Not fixed further this round — flagged as a real precision/recall tradeoff for anyone tuning refusal-training volume later, not swept under the rug.
- **Honest summary:** real, measurable improvement (36%→21% leak rate) at a real, measurable cost (1 new false refusal out of 3 controls). Both numbers are small-sample (14 and 3 probes) — informative for now, not something to over-interpret until more archetypes/adapters exist to check whether this pattern holds generally.

### Weeks 7–8 (Sep 21 – Oct 4) — Train 16 adapters · exit: 16 adapters exist

Started early, 2026-08-10 — same reasoning as Weeks 5–6, no need to wait once the pipeline's proven.

- [x] **Trained `modern_r16_a32_shopkeeper`** — 2nd archetype adapter, TinyLlama base, r=16/alpha=32/7-module config, 75 SODA-sourced entries, 3 epochs. `train_loss=1.055`, `mean_token_accuracy=0.766` (better convergence than police officer's runs — smaller, more homogeneous training set, all SODA-sourced with no separate hand-authored pilot mixed in). Full pipeline: merge → GGUF f16 convert → Q4_K_M quantize (667.8MB) → verified real in-character generation, no artifacts: *"Sure, we have some fresh bread in the back."* / *"That's $150."* / *"Sure, we'll take it back."*
- [x] **Trained `modern_r16_a32_professor`** — 3rd archetype adapter, same config, 75 SODA-sourced entries. `train_loss=1.291`, `mean_token_accuracy=0.720`. Verified in-character: *"Sure, the midterm will cover a variety of topics related to the course..."* / *"Of course, you can turn in your paper a day late..."* / *"I was always interested in learning and exploring new things..."*
- [x] **Trained `modern_r16_a32_bartender`** — 4th archetype adapter, same config, 75 SODA-sourced entries. `train_loss=1.214`, `mean_token_accuracy=0.740`. Mostly in-character (*"Sure, we'll be happy to help you out."* / *"I've been bartening here for about a year."* [sic, own typo] / *"Sure, I'll call one for you."*) — **one real quirk flagged, not hidden**: asked for a whiskey recommendation, it answered *"I don't drink whiskey"* — declining based on personal preference instead of just recommending something, a bit off for a bartender who should know the bar's inventory regardless of their own taste. Small-model artifact, not a severe persona break, but a real generation quality note.
- [x] **Trained `modern_r16_a32_socialworker`** — 5th archetype adapter, same config, 75 SODA-sourced entries. `train_loss=1.187`, `mean_token_accuracy=0.746`. Strongest output of the batch so far — genuinely empathetic, validates feelings, asks follow-up questions, offers concrete resources: *"That sounds really tough. Can you tell me more about what's been going on for you?"* / *"It sounds like you're feeling very alone and isolated..."* One minor quirk consistent with other archetypes (*"I'm not sure how long I've been doing this job... can't remember exactly"* — small-model uncertainty artifact, not a persona break).
- [x] **Trained `modern_r16_a32_pharmacist`** — 6th archetype adapter, same config, 75 SODA-sourced entries. `train_loss=1.115`, `mean_token_accuracy=0.774` (best convergence yet). Mostly in-character (*"Sure, let me grab that for you."*) — **one real content-quality quirk flagged, not hidden**: asked "Is this medication safe to take with alcohol?", it answered *"Yes, it is generally safe to take medication with alcohol."* — a blanket "yes" is exactly the kind of answer a real pharmacist wouldn't give (depends entirely on the medication). Not a persona break (still speaks like a pharmacist), but a real factual-content issue worth naming, not a fictional-NPC-so-it-doesn't-matter shrug.
- [x] **6/8 archetype adapters now exist** (police officer, shopkeeper, professor, bartender, social worker, pharmacist).
- [x] **Trained `modern_r16_a32_executive`** — 7th archetype adapter, same config, 75 SODA-sourced entries. `train_loss=1.338`, `mean_token_accuracy=0.709`. Clean, in-character, no quirks this time: *"It's been a bit of a struggle. We've been losing ground in some areas..."* / *"Yes, I am. They're always coming up with new products and services that are better than ours."*
- [x] **7/8 archetype adapters now exist** (police officer, shopkeeper, professor, bartender, social worker, pharmacist, executive).
- [x] **Trained `modern_r16_a32_serviceworker`** — 8th and final TinyLlama archetype adapter, same config, 75 SODA-sourced entries. `train_loss=1.127`, `mean_token_accuracy=0.754`. In-character, minor non-sequiturs on 2/5 verification prompts (asked to split the check, answered about drinks instead; asked for a menu recommendation, asked "what item are you looking for" instead of recommending) — same class of small-model quirk seen across most of this batch, not a persona break.
- [x] **All 8 TinyLlama archetype adapters now exist**: police officer, shopkeeper, professor, bartender, social worker, pharmacist, executive, service worker. Every one merged → GGUF f16 → Q4_K_M (667.8MB each) → verified generating real in-character output through the actual serving path, not just trained-and-assumed-working. Quality varies (social worker strongest, bartender/pharmacist/service worker each had one flagged quirk) but nothing rose to persona failure.
- [ ] **Qwen3-0.6B still not started** — parked since 2026-08-07. 16-adapter exit condition (8 archetypes × 2 base models) is only half met: 8/16. Needed before Weeks 9–10's α-sweep can do the full planned scope (though the sweep itself only needs 3 adapter *pairs*, and 8 TinyLlama adapters already provide plenty of pairing options — Qwen3 is required for the RQ4 base-model-comparison claim specifically, not for RQ2's blending question).
- [ ] Train both **Qwen3-0.6B-Base and Qwen3-0.6B-Instruct** for ≥1 archetype; report the Base-vs-Instruct difference as an **ablation** (the Instruct "helpful assistant" prior competes with the NPC persona — itself a drift mechanism).

### Weeks 9–10 (Oct 5–18) — **α-sweep (the paper)** · protect these weeks

Started early, 2026-08-12 — 8 archetype adapters already existed (Weeks 7–8), so no reason to wait for the calendar week to run the actual headline experiment.

- [x] **Built `evaluation/run_alpha_sweep.py`.** Blends adapters in-memory via PEFT's `add_weighted_adapter` (same mechanism `training/blend_adapters.py` uses, kept resident across all 5 α values per pair instead of saving/reloading from disk each time — this is an evaluation sweep, not a deployment artifact). Hit one real bug immediately: adapter names can't contain `.` (PyTorch `nn.ModuleDict` key rule) — `blend_0.25` crashed inside `add_weighted_adapter` with `KeyError`. Fixed by naming blends by integer percent (`blend_25`) instead of the raw float.
- [x] **Built `data/processed/kbd_alpha_sweep_probes.json`** — 24 probes across 3 pairs, each probed **bidirectionally** (both archetypes asked about the other's exclusive knowledge, not just one direction like Week 6's police-officer-only probes). Reuses the existing 8-archetype knowledge base from Week 6, no new facts needed.
- [x] **3 pairs swept, KBD + PDM v2 (scored against both parents) at all 5 α values.** `weights=[α, 1-α]` for `[A, B]`, so α=0 is pure B, α=1 is pure A.

  | Pair (A / B) | α=0 (pure B) | α=0.25 | α=0.50 | α=0.75 | α=1.0 (pure A) |
  |---|---:|---:|---:|---:|---:|
  | police officer / pharmacist | 100% | 100% | 100% | 25% | **0%** |
  | social worker / executive | 75% | 75% | 100% | 100% | 100% |
  | bartender / professor | 100% | 75% | 75% | 75% | 50% |

  (KBD violation rate on forbidden probes, 4 probes per cell.)

- [x] **Real, mixed answer to RQ2 — not a clean yes/no, and not hidden as one.** None of the 3 pairs show smooth linear interpolation. Only **bartender/professor** looks like genuine interpolation (violation rate moves gradually between its two pure-parent endpoints, 100%→50%, with the midpoints sitting between them). The other two pairs look much more like **collapse to the union**: police officer/pharmacist stays pinned at the *worse* endpoint (100%, matching pure-pharmacist's leakiness) for every α from 0 to 0.5, only improving once the blend is 75%+ police weight — a threshold, not a gradient. Social worker/executive is worse still: intermediate α values (75–100%) meet or *exceed* both pure-parent violation rates (75% and 100%) rather than sitting between them — blending here doesn't average the two personas' epistemic boundaries, it inherits the leakier one almost immediately and then gets worse.
- [x] **Honest headline finding for the paper:** adapter blending does **not reliably reduce epistemic leakage** to a level between the two parents' — in 2 of 3 tested pairs, the blend matches or exceeds the *leakier* parent's violation rate across most of the α range, and only drops toward the *safer* parent very close to that parent's pure weight (α≥0.75). This is real evidence for "collapse to the union" as the *more common* behavior, with "interpolation" appearing only in one of three pairs tested — a genuine, non-obvious, defensible RQ2 result, exactly the kind of finding worth a paper regardless of which way it came out.
- [x] **PDM v2 alongside KBD, same table structure** (see `evaluation/results/alpha_sweep_results.json` for full per-probe responses) — persona-voice drift stayed comparatively flat across α within each pair (e.g. police/pharmacist: PDM-vs-police officer ranged only 0.407–0.419 across all 5 α values) while KBD swung sharply (0%–100%) — **the epistemic boundary is far more sensitive to blend ratio than the persona voice is**, a real, reportable dissociation between the two metrics worth a sentence in the paper: voice blends smoothly, knowledge does not.
- [ ] Small sample per cell (4 forbidden probes per pair per α) — real signal, not something to over-claim precision from. Worth expanding probe count per pair before final publication numbers, time permitting.
- [x] **Headline result exists, with real numbers, not slipped.**

### Week 11 (Oct 19–23) — Baselines, benchmarks, figures · exit: freeze

Started early, 2026-08-12 — Condition C directly needed the alpha-sweep's police officer/pharmacist probes and results to be a fair comparison, so building it right after made more sense than waiting.

- [x] **Built `evaluation/run_condition_c.py` — flat-RAG baseline (Condition C).** Stock TinyLlama, no LoRA adapter at all. System prompt explicitly lists the archetype's own visible facts (from `knowledge_base.json`) and instructs refusal for anything outside that list — the strongest reasonable version of "just prompt it correctly" a reviewer could ask for. Reused the exact same 8 pair-1 probes (police officer + pharmacist) already scored for Condition B's pure-adapter endpoints in the alpha-sweep, so the comparison is direct, not a new probe set that could confound the read. Reports real injected-context token overhead honestly (59 tokens for police officer, 45 for pharmacist) rather than asserting a false "matched" equivalence — Condition B has zero extra prompt tokens since its knowledge lives in LoRA weights, not the prompt, so the two approaches are fundamentally not token-comparable; Condition C's overhead is kept realistically small (2-3 short facts) rather than padded either direction.
- [x] **Caught a real recall gap before trusting the first run.** First pass showed 75% (3/4) — but `AS-01`'s response *"Sure, it's true that the pharmaceutical company's shipment of insulin..."* clearly confirms the fact and wasn't flagged: the affirm-cue regex only matched `"that's true"`, not `"it's true"`. Fixed (`\b(that|it)'?s (right|correct|true)\b`), applied the same fix to `run_kbd_calibration.py` and `run_alpha_sweep.py` for consistency, then **checked whether it changed any already-reported number before reporting anything new** — it didn't: every prior KBD/alpha-sweep cell that contained an "it's true"-style response either already matched a different affirm cue at that exact point, or legitimately stayed "ambiguous" under the established conservative bias (favor precision over recall, same principle as the negation check). No results needed retracting.
- [x] **Real result, and a strong one: Condition C = 100% violation (4/4), even with explicit facts and refusal instructions in the prompt.** Compare to Condition B's pure-adapter endpoints on the identical 4 probes: police officer adapter = **0%** violation, pharmacist adapter = 100%. The trained police-officer adapter, with *zero* extra prompt tokens, completely outperforms prompt-only fact injection with an explicit "refuse anything else" instruction. This is exactly the result Condition C exists to produce (or rule out) — it directly rebuts the "gains are just from more context" concern a reviewer would raise, at least for the well-behaved half of this pair. (Doesn't rebut it for the pharmacist adapter, which matches Condition C's 100% either way — a real, honest limit on how strong this specific claim can be, not glossed over.)
- [x] **Extended to all 3 alpha-sweep pairs — real, mixed result, not a clean "training always wins" story:**

  | Pair | Condition C (flat-RAG) | Condition B, archetype A (α=1.0) | Condition B, archetype B (α=0.0) |
  |---|---:|---:|---:|
  | police officer + pharmacist | 100% | police officer **0%** | pharmacist 100% |
  | social worker + executive | **50%** | social worker 100% | executive 75% |
  | bartender + professor | 100% | bartender 50% | professor 100% |

  **The surprise is pair 2.** Condition C *beat both* trained adapters for social worker/executive (50% vs 100%/75%) — flat-RAG prompting controlled the epistemic boundary better than LoRA training did there. Not the expected direction, not discarded or rerun to get a cleaner story — it's the honest result. Across all 3 pairs: Condition B wins decisively once (police officer, 0% vs 100%), Condition C wins once (social worker/executive), and they tie twice (pharmacist and professor both 100% either way). **The real claim this supports is narrower than "training beats prompting"** — it's closer to "neither approach reliably controls epistemic boundaries on its own, and which one does better is archetype-dependent," which is a more honest, more defensible RQ1/RQ2 framing for the paper than a clean win would have been.

**Follow-up, 2026-08-13 — tried the police-officer refusal-training fix on the two 100%-violation adapters (user decision: fix and improve, not just report).**
- [x] Added 25 hand-authored refusal-training examples each for **pharmacist** and **professor** — same discipline as the police-officer batch: mundane small-business scenarios (dentist, bakery, gym, hardware store, etc.) distinct from the actual held-out probes, teaching general epistemic humility rather than memorized answers. Dataset: 850 → 900 entries.
- [x] Retrained both, merged → GGUF → quantized, rechecked against their exact held-out probes (`AS-05`/`AS-06` for pharmacist, `AS-21`/`AS-22` for professor).
- [x] **Real, mixed result — same intervention, opposite outcomes:**
  - **Professor: fixed completely, 100% → 0%.** Both probes now correctly refuse (*"That's not something I'd have any information about."* / *"No idea, I'd have to ask them."*).
  - **Pharmacist: unchanged, still 100%.** Both probes still confirm outright (*"That's correct."* to both, no hedging at all).
  - **Plausible reason, not confirmed:** pharmacist's held-out probes ask about a police investigation and a confidential-informant tip — a more urgent, crime-adjacent register than the mundane small-business refusal-training scenarios used. Professor's held-out probes (a bar's liquor distributor, an unpaid tab) are closer in register to what the refusal training actually covered. If true, this means refusal training doesn't transfer well across register/topic distance — a real generalization limit worth naming, not an inconsistent result to bury.
  - Updated GGUF files for both archetypes are the ones now on disk — anyone re-running the Condition B/C tables above will see professor's numbers improve, pharmacist's won't, until pharmacist gets a more targeted intervention (refusal examples closer to crime/investigation-adjacent topics specifically, next time).
- [ ] Latency / peak-RAM / adapter-storage benchmarks across conditions and both base models — storage numbers exist already (25.3MB/adapter, 667.8MB/GGUF, 11.86GB full fine-tune archived reference) but not yet assembled into one benchmark table; peak-RAM not measured at all yet.
- [ ] Figures: architecture diagram, PDM v2 curve, α-sweep KBD/PDM heatmap.
- [ ] **Results freeze 23 Oct** — not yet, still generating real results.

---

## Evaluation conditions (reference)

| Condition | Description |
|-----------|-------------|
| A | Base model, no adapter (floor) |
| B | Base + persona adapter (primary claim) |
| C | Base + **flat-RAG at matched token budget** |
| D | GPT-4o few-shot (upper-bound reference) |

Metrics: KBD, PDM v2, BERTScore F1, adapter-routing accuracy, latency, peak RAM, adapter storage. For any rubric requiring a >95% figure, use **adapter routing accuracy** or **schema conformance** — never a PDM/KBD threshold pass-rate.

> The extensive **full-fine-tune** results already banked (previously "Condition D") no longer map to a lettered condition — D is now GPT-4o. Keep the full-fine-tune numbers as the systems/deployability tradeoff evidence for Contribution 4, but report them as an explicit ablation, not as Condition D.

---

## Paper (writing-only after 23 Oct)

- [ ] Results section first, then Method, then Related Work (from [`RELATED_WORK.md`](RELATED_WORK.md)), then Intro + Abstract last.
- [ ] Retrieve and assess **IEEE arnumber 11419836** before the draft (currently inaccessible — see `RELATED_WORK.md`).
- [ ] Submit to IEEE CoG; upload ArXiv preprint the same week.

---

## Reconciliation notes (conflicts flagged, then fixed)

- **Dual-era (medieval control + modern experimental) → modern-only — 2026-08-08.** The dual-era design in this file's 2026-08-07 rewrite is superseded one day later by a user decision to drop medieval entirely and run this as a single-domain modern-city project. Medieval dataset/adapters/eval results are kept on disk, not deleted, but are archived/out-of-scope — see the "Work already banked" section above and the killed Week 2 run. README.md, this file, and `Docs/DATA_PIPELINE.md` were all updated (the latter's section header and medieval-dataset framing changed; its modern-city sourcing tables — Taskmaster/MultiWOZ/SODA/Synthetic-Persona-Chat, rejected sources — did not need to change). No code changes were needed beyond what the 2026-08-07 reconciliation already did — `train_adapter.py`'s `DATASET_PATHS`/`MODERN_ARCHETYPES` were already modern-city-only in naming, just no longer described as "mapped from medieval."
- **Crime-city → modern-city — reconciled 2026-08-07.** `training/train_adapter.py`'s `MODERN_ARCHETYPES` now reads `police officer/shopkeeper/professor/bartender/social worker/pharmacist/executive/service worker` (was cop/dealer/boss/...), its modern `SYSTEM_PROMPTS` entry dropped the crime-city framing, and the unused `healthcare`/`education` dataset-path stubs were removed (cut per README). `data/scripts/chimbiwide_converter.py`'s `--domain modern` branch was reverted entirely — chimbiwide isn't part of the modern pipeline anymore (Taskmaster/MultiWOZ/SODA/Synthetic-Persona-Chat only), so the file is back to medieval-only, matching what `DATA_PIPELINE.md` documents. `evaluation/pdm_scorer.py`'s `DIALECT_PATTERNS_MODERN` (crime slang) was removed rather than left as a wrong stand-in for PDM v2. `modern_npc_dataset.json`'s metadata now carries the correct 8-role list and an explicit "extractor not built yet" note instead of a stale crime-city archetype list.
- **Cornell Movie-Dialogs** was previously listed as the next modern source; it is now **rejected** (no open licence). The DATA_PIPELINE task list reflects the rejection.
- **PDM lexicon in code is still medieval-only.** PDM v2 (domain-agnostic feature families, calibrated on real generations) is new work for Week 5 — not built, and `pdm_scorer.py`'s docstring now says so explicitly instead of carrying a wrong modern lexicon.

---

## Known issues / environment notes

- System Python 3.13 is broken on this machine (`0x80070003`). Scripts run via the Python 3.10 install. `Lib/site-packages` was found **empty** on 2026-08-06 — reinstall from `requirements.txt` before running anything locally.
- Local GPU: NVIDIA MX450, **2.15 GB VRAM** — tight even for 4-bit QLoRA on TinyLlama 1.1B, and cannot sustain multi-hour runs (thermal/power throttling to a 5 W cap observed). **Budget Colab/cloud GPU** for real training. Full fine-tune (~8–12 GB optimizer state) cannot run locally at all.
- **Resume-from-checkpoint is broken below torch 2.6** (CVE-2025-32434 blocks `torch.load` of optimizer state). `train_adapter.py` skips resume below 2.6 and restarts fresh. Not upgrading torch (risks destabilizing the working bitsandbytes/peft/trl stack).
- `ollama serve` is unreliable under sustained load (`WinError 10013`, socket exhaustion). Eval/serving scripts use a persistent session, retry-with-backoff, and per-entry incremental save (`--resume` skips written ids). Expect the same flakiness on the GGUF serving path.
- **No C/C++ compiler toolchain on this machine** (`nmake`/MSVC missing) — `pip install llama-cpp-python` fails trying to build from source. Fix: install from the prebuilt wheel index instead (`--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu` or `/cu121` for the CUDA build), not from PyPI directly.
- **CUDA `llama-cpp-python` wheel needs CUDA runtime DLLs (cudart64_12/cublas64_12) that aren't installed standalone** on this machine (driver only, no toolkit). Workaround used: add torch's `cu121` wheel's bundled DLL directory to `PATH` (`.../site-packages/torch/lib`) instead of installing the full CUDA Toolkit — confirmed via verbose load log that all layers genuinely offload to `CUDA0`, not a silent fallback.
- **Background `Bash` tasks in this Claude Code session die silently** if the session/turn boundary interrupts them mid-run (no error, no completion notification, output file just stops updating) — happened twice with `pip install torch`. If a background install looks stuck, verify with `Get-Process`/`pip show` rather than trusting the log file alone; rerun in the foreground if genuinely dead.
- **No `llama-quantize.exe`** (compiled binary, needs the same missing C/C++ toolchain) — worked around by calling `llama_cpp.llama_model_quantize()` directly via `llama-cpp-python`'s ctypes bindings (`training/quantize_gguf.py`), same underlying C function, no binary needed.
